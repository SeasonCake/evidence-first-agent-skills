interface:
  display_name: "Architecture Survey"
  short_description: "Survey a bounded subsystem and its structural seams"
  default_prompt: "Use $architecture-survey to produce a read-only architecture survey for this subsystem."

policy:
  allow_implicit_invocation: false
