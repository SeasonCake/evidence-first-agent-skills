# Scoped compatibility evidence

Recorded 2026-09-13; model-picker evidence added 2026-09-14. The
[2026-09-15 reliability review](../../docs/GROK_RELIABILITY_REVIEW_2026-09-15.md) adds
parent-owned waits, original readback, role context, actual permission controls and
manual-compact recovery. These are results for the inspected local host, not promises about
every Codex/Grok version or a claim of supported native cross-provider Subagents parity.

| Surface | Evidence | Limit |
| --- | --- | --- |
| Persistent desktop task | One real initialization, independent saved readback, direct native-app continuation | Ready is not business completion or permission inheritance |
| Tools and warm summary | Read-only shell calculation and same-task marker/summary passed after normal reopen | No product or all-tool guarantee |
| Runtime context | Creation and native continuation reported 475000 after the startup catalog repair; native parent kept its selected 760000 | Automatic threshold and long-session endurance not exercised |
| Native model preservation | All eight original host-loaded rows matched field-for-field; one Grok row added | Refresh/recheck is needed after model/host changes |
| Completion return, September 13 | One finite Luna/high observer continued a yielded command and its exact-turn result reached the bound parent | Historical selected observer; current active-parent workflow uses bounded wait plus original `receive` |
| Manual compaction, September 13 | Real compaction completed; afterwards the synthetic marker and an actual dynamic-tool call/result survived | The original production failure request was unavailable; no unique-cause claim |
| Manual compaction, September 15 | Missing custom-call item ID reproduced offline; after the targeted patch/reload, a real compact completed in 128 seconds and changed-rule continuity passed | No guarantee for unrelated compact errors or every long-session history |
| Parent wait and full result, September 15 | Native exact-turn wakes plus validated original JSON/byte hashes | Active parent only; no automatic offline/restart return |
| Permission inspection | Exact-turn fields can be read without modifying settings | Factory parent binding does not automatically inherit parent permissions |
| Offline checks | Portable Python protocol/configuration controls and optional installed-source Bun checks | Synthetic tests do not replace real provider/host acceptance |
| Native model button | Local desktop 26.903.8094.0 / CLI 0.153.4: new Grok task, same-task GPT/Grok context recall and return to GPT, then actual UI controls after reopen | The router is opt-in; a host upgrade needs a launch/protocol check |
| Older Grok menu entries | One basic no-tools chat each for 4.5 and 4.3 in the inspected local setup | Not full tool, context or model-quality acceptance; public setup starts with the selected row |
| Windows bootstrap | Source-level quoting and stdio checks with synthetic local fixtures | Generated executables are not distributed or installed automatically |

The initial compaction test waited for a legacy notification name and timed out after
the host had already emitted a `contextCompaction` item. The corrected harness waited
for that actual item plus terminal turn status before continuing. This was a test-event
contract error, not evidence that the upstream compaction itself failed.

The single-row catalog result above describes the earlier setup; the later local model
button control added three selected Grok rows while preserving the native rows. The
public helper supports explicit multiple selections but does not auto-discover or enable
every advertised model. See [model-picker setup](MODEL_PICKER.md).

Native Grok CLI media, a Side chat redesign, automatic permission
inheritance and account-level token savings are not certified by this package. The
parent wait/reader and any separately selected observer have overhead; host token counters
are not provider billing. The image test's auxiliary denied-tool failure is preserved in
the newer review alongside its valid artifact and the later guide-loading control.
