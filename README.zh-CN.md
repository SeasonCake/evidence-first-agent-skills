# Evidence-first agent skills（公开候选）

本仓包含四个显式调用的小 skill：只读架构调查、单一主张验证、CLI 合同复核与新代理兼容性检查。
它们来自真实项目推进中的失败教训，但已去项目名、路径、私有证据和生产信息。

```powershell
python scripts/verify.py
```

上游来源和本地改造分别记录在 `UPSTREAM.md` 与每个 skill 的 `ATTRIBUTION.md`。最终许可证仍待作者
冻结。
